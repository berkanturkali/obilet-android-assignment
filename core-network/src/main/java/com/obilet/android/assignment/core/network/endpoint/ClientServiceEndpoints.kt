package com.obilet.android.assignment.core.network.endpoint

/**
 * Contains endpoints related to
 * /client route/path
 */
object ClientServiceEndpoints {

    private const val CLIENT_PATH = "client/"

    const val GET_SESSION_ENDPOINT = CLIENT_PATH + "getsession"
}